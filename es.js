const first = "sumoya";
const last = "akter";
// const name = first + " " + last;

const name = `${first}  akter ${last}`;
console.log(name);

const songName = "amer sonar bangla ami \n tomay valobaci";
console.log(songName);

const numbers = [20, 33, 66, 55, 88];
const student = { name: "maria", age: 12 };

const math = `the sum of ${numbers[0]} and ${student.age} is ${
  numbers[0] + student.age
} `;

console.log(math);
